SUPER_CONFIG_PATH: outputs/bytetrack_fa_mot_mot17_v10_tune_matchloss_val0405_allDet_fix27_longseq_len6/config_effective.yaml

# Debug / fast eval config:
# - Evaluate only val sequences (04/05)
# - Only DPM detector variants (fast iteration for public-det tuning)
INFERENCE_DATASET: MOT17
INFERENCE_SPLIT: train
EVAL_ONLY_VAL: true
VAL_SEQUENCES: ["MOT17-04", "MOT17-05"]
DETECTOR_FILTER: ["DPM"]

# Use public detections (det/det.txt). Can still override via CLI.
BYTETRACK_DET_SOURCE: public

# Keep thresholds unset here; tune via CLI / sweep script.
