SUPER_CONFIG_PATH: configs/bytetrack_fa_mot_mot17_v10_fromscratch_unfreezeproj_longcurr_p0fix_matchloss.yaml

# v10 tune config (fix27_longseq_len6): long-range curriculum + shorter epoch length
# - Start with longer temporal windows earlier to improve long-term association
# - Use LENGTH_PER_ITERATION to keep epoch runtime practical as sample_length increases
EXP_NAME: bytetrack_fa_mot_mot17_v10_tune_matchloss_val0405_allDet_fix27_longseq_len6

DETECTOR_FILTER: null
VAL_SEQUENCES: [MOT17-04, MOT17-05]
ID_THRESH: 0.003
LOGGING_INTERVAL: 20

# === Longer temporal context ===
MAX_SEQ_LEN: 48
SAMPLE_STEPS: [0, 10, 25]
SAMPLE_LENGTHS: [18, 24, 30]
SAMPLE_INTERVALS: [4, 6, 8]

# Keep epoch length roughly proportional to the amount of frames processed per step.
# This reduces the number of sampled windows per epoch by (sample_length / LENGTH_PER_ITERATION),
# which is critical when increasing SAMPLE_LENGTHS.
LENGTH_PER_ITERATION: 6

