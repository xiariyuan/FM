SUPER_CONFIG_PATH: configs/bytetrack_fa_mot_mot17_v10_tune_matchloss_val0405_allDet_fix27_longseq_len6.yaml

# v11b: Continue from v11 checkpoint (epoch 29) after a server reboot, but undo the overly
# aggressive DET_MAX_PER_FRAME cap (40) that hurts recall/validation.
#
# Also switch to a low-LR fine-tuning phase to avoid further drift while letting the model
# settle.
EXP_NAME: bytetrack_fa_mot_mot17_v11b_resume_e29_g2_det50_bs4_acc2_lr5e5

RESUME_MODEL: outputs/bytetrack_fa_mot_mot17_v11_resume_e20_g2_det40_bs4_acc2/checkpoint_29.pth
RESUME_OPTIMIZER: false
RESUME_SCHEDULER: false

# Fine-tune schedule (resume will start at epoch 30)
EPOCHS: 40
LR: 0.00005
LR_WARMUP_EPOCHS: 0

# OOM safety / effective batch
USE_DECODER_CHECKPOINT: true
AUG_NUM_GROUPS: 2
DET_MAX_PER_FRAME: 50

BATCH_SIZE: 4
ACCUMULATE_STEPS: 2

